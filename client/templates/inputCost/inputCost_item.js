Template.inputCostItem.helpers({
  /*formattedCost: function(cost) {
    formatted = accounting.formatMoney(cost, "$", 2);
    return formatted;
  }, 
  notLabor: function(material) {
    if(material.type === 'wood')
    	return true;
    else
    	return false;
  }, */
  

});